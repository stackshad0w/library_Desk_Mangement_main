{
  "name": "swami-abhyasika-vercel",
  "version": "1.0.0",
  "description": "Swami Abhyasika — Vercel serverless + Turso (libSQL) + Cloudinary build. Same app, same UI, zero app.js changes.",
  "private": true,
  "scripts": {
    "seed": "node scripts/seed.js",
    "migrate": "node scripts/migrate.js",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "vercel": "^37.0.0"
  }
}
